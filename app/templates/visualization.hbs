<Visualization::Frame
  @items={{this.model}}
  @loaded={{this.loaded}}
  @labels={{this.labels}}
  @properties={{this.properties}}
  @searchTerms={{this.searchTerms}}
  @clearCanvas={{action "clearCanvas"}}
  @undoLoad={{action "undoLoad"}}
/>