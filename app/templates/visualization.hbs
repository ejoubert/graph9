<Visualization::canvasFrame
  @items={{this.model}}
  @loaded={{this.loaded}}
  @label={{this.label}}
  @property={{this.property}}
  @searchTerm={{this.searchTerm}}
  @clearCanvas={{action "clearCanvas"}}
/>