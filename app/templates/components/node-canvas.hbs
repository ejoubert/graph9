<hr>
{{#visjs-network}}
  {{#each model as |node|}}
    {{#if node.isNode}}
      {{node-view node=node}}
    {{else}}
      {{visjs-edge from=node.start to=node.end arrows='to' select=(action 'selectEdge')}}
    {{/if}}
  {{/each}}
{{/visjs-network}}
<hr>